<div class="min-h-screen pt-20 pb-12" style="background: radial-gradient(ellipse at center, rgba(255,255,255,0.02) 0%, rgba(0,0,0,0) 40%), linear-gradient(180deg,#1b0b1b 0%, #100615 100%);">
      <main class="lg:col-span-9">
        <div class="bg-gradient-to-b from-[#251428]/80 via-[#1b0f1a] to-[#0f0810] border border-[#3a243a] rounded-2xl p-6 shadow-lg">
          <h1 class="text-2xl font-bold text-purple-300 mb-2">Dashboard</h1>
          <p class="text-sm text-gray-300 mb-6">Bienvenido, <strong class="text-white"><%= user?.username || 'Usuario' %></strong>. Aquí puedes administrar tus servidores y configurar el bot.</p>

          <div class="p-6 bg-gradient-to-b from-[#241127]/30 to-transparent border border-[#3a243a] rounded-xl">
            <h2 class="text-2xl font-semibold text-purple-300 mb-4">Servidores asociados</h2>
            <% if (Array.isArray(guilds) && guilds.length > 0) { %>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <% guilds.forEach(function(g) { %>
                  <div class="flex items-center justify-between p-4 rounded-lg border border-[#3a243a] bg-gradient-to-b from-[#1a0b18]/50 to-[#150812]/30">
                    <div>
                      <div class="font-medium text-white"><%= g.name %></div>
                      <div class="text-xs text-gray-400">ID: <%= g.id %></div>
                    </div>
                    <div>
                      <a href="/dashboard/guild/<%= g.id %>" class="text-sm text-indigo-300 hover:underline">Abrir</a>
                    </div>
                  </div>
                <% }) %>
              </div>
            <% } else { %>
              <div class="text-sm text-gray-400">No hay servidores asociados a tu cuenta.</div>
            <% } %>
          </div>
        </div>
      </main>
    </div>
  </div>
</div>
